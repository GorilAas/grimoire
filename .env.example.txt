@"
# Banco de dados
POSTGRES_DB=grimoiredb
POSTGRES_USER=grimoire
POSTGRES_PASSWORD=sua_senha_aqui

# Backend
SPRING_PROFILES_ACTIVE=docker
"@ | Out-File -FilePath ".env.example" -Encoding ascii
```

Adiciona o `.env` no `.gitignore` da raiz — abre o arquivo e confirma que tem essa linha:
```
.env